# 前端 UI 组件实现详解

## 概述

这一部分详细解释如何构建用户友好的同步界面，包括连接流程、同步操作和状态反馈。

---

## 🎨 UI 设计

### 界面流程图

```
┌─────────────────────────────────────┐
│  用户点击 "Calendar Sync" 按钮      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     打开 CalendarSyncDialog         │
└──────────────┬──────────────────────┘
               │
               ▼
         ┌──────────┐
         │ 已连接？  │
         └───┬──────┘
             │
    ┌────────┴────────┐
    │                 │
   NO               YES
    │                 │
    ▼                 ▼
┌────────┐     ┌──────────────┐
│显示连接│     │显示同步界面  │
│引导界面│     │  - 账户信息  │
└────────┘     │  - 同步按钮  │
               │  - 同步历史  │
               └──────────────┘
```

---

## 📦 组件结构

### CalendarSyncDialog.tsx 完整实现

```typescript
"use client"

import { useState, useEffect } from "react"
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Cloud, Calendar, Sync, CheckCircle, XCircle, AlertCircle, Loader2 } from "lucide-react"
import { useSubscription } from "@/components/SubscriptionContext"
import { toast } from "sonner"

interface SyncStatus {
  connected: boolean
  syncing: boolean
  accountEmail?: string
  lastSyncTime?: string
  syncedEventCount?: number
}

interface SyncResult {
  success: boolean
  message: string
  synced: number
  failed: number
  errors?: Array<{ eventId: string; error: string }>
}

export function CalendarSyncDialog({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) {
  const { isPro } = useSubscription()
  const [status, setStatus] = useState<SyncStatus>({
    connected: false,
    syncing: false,
  })
  const [syncResult, setSyncResult] = useState<SyncResult | null>(null)
  const [loading, setLoading] = useState(true)

  // 加载连接状态
  useEffect(() => {
    if (open) {
      checkStatus()
    }
  }, [open])

  // 检查连接状态
  const checkStatus = async () => {
    setLoading(true)
    try {
      const response = await fetch('/api/calendar/status')
      if (response.ok) {
        const data = await response.json()
        setStatus(data)
      }
    } catch (error) {
      console.error('Failed to check status:', error)
    } finally {
      setLoading(false)
    }
  }

  // 连接 Google Calendar
  const handleConnect = async () => {
    try {
      const response = await fetch('/api/calendar/auth')
      const { authUrl } = await response.json()

      // 重定向到 Google 授权页面
      window.location.href = authUrl
    } catch (error) {
      console.error('Failed to get auth URL:', error)
      toast.error('无法连接到 Google Calendar，请稍后重试')
    }
  }

  // 断开连接
  const handleDisconnect = async () => {
    if (!confirm('确定要断开 Google Calendar 连接吗？这不会删除已同步的事件。')) {
      return
    }

    try {
      const response = await fetch('/api/calendar/disconnect', { method: 'POST' })
      if (response.ok) {
        toast.success('已断开 Google Calendar 连接')
        setStatus({ connected: false, syncing: false })
      } else {
        toast.error('断开连接失败，请稍后重试')
      }
    } catch (error) {
      console.error('Failed to disconnect:', error)
      toast.error('断开连接失败，请稍后重试')
    }
  }

  // 执行同步
  const handleSync = async () => {
    setSyncResult(null)
    setStatus(prev => ({ ...prev, syncing: true }))

    try {
      const response = await fetch('/api/calendar/sync', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ mode: 'incremental' }),
      })

      const result: SyncResult = await response.json()
      setSyncResult(result)

      if (result.success) {
        toast.success(result.message)
        // 重新获取状态
        await checkStatus()
      } else {
        toast.error(result.message)
      }
    } catch (error) {
      console.error('Sync failed:', error)
      toast.error('同步失败，请稍后重试')
    } finally {
      setStatus(prev => ({ ...prev, syncing: false }))
    }
  }

  // 格式化时间
  const formatTime = (dateString: string) => {
    const date = new Date(dateString)
    const now = new Date()
    const diffMs = now.getTime() - date.getTime()
    const diffMins = Math.floor(diffMs / 60000)

    if (diffMins < 1) return '刚刚'
    if (diffMins < 60) return `${diffMins} 分钟前`
    if (diffMins < 1440) return `${Math.floor(diffMins / 60)} 小时前`
    return `${Math.floor(diffMins / 1440)} 天前`
  }

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-[500px]">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2">
            <Calendar className="size-5" />
            Google Calendar 同步
          </DialogTitle>
        </DialogHeader>

        <div className="space-y-6 py-4">
          {loading ? (
            // 加载中
            <div className="flex flex-col items-center justify-center py-8">
              <Loader2 className="size-8 animate-spin text-blue-600" />
              <p className="mt-4 text-sm text-gray-600">加载中...</p>
            </div>
          ) : !status.connected ? (
            // 未连接状态
            <div className="space-y-4">
              <div className="flex items-start gap-3 p-4 bg-blue-50 rounded-lg border border-blue-200">
                <Cloud className="size-5 text-blue-600 mt-0.5" />
                <div className="flex-1">
                  <h3 className="font-medium text-blue-900">连接 Google Calendar</h3>
                  <p className="mt-1 text-sm text-blue-700">
                    将你的日程同步到 Google Calendar，方便在所有设备上查看。
                  </p>
                </div>
              </div>

              <div className="space-y-2 text-sm text-gray-600">
                <p className="flex items-start gap-2">
                  <CheckCircle className="size-4 text-green-600 mt-0.5 shrink-0" />
                  <span>自动同步所有事件到 Google Calendar</span>
                </p>
                <p className="flex items-start gap-2">
                  <CheckCircle className="size-4 text-green-600 mt-0.5 shrink-0" />
                  <span>支持多天事件和循环事件</span>
                </p>
                <p className="flex items-start gap-2">
                  <CheckCircle className="size-4 text-green-600 mt-0.5 shrink-0" />
                  <span>保持颜色、位置等所有信息</span>
                </p>
              </div>

              <Button
                onClick={handleConnect}
                className="w-full bg-blue-600 hover:bg-blue-700"
              >
                <Cloud className="size-4 mr-2" />
                连接 Google Calendar
              </Button>
            </div>
          ) : (
            // 已连接状态
            <div className="space-y-4">
              {/* 连接信息 */}
              <div className="flex items-center gap-3 p-4 bg-green-50 rounded-lg border border-green-200">
                <CheckCircle className="size-5 text-green-600" />
                <div className="flex-1">
                  <h3 className="font-medium text-green-900">已连接</h3>
                  <p className="text-sm text-green-700">{status.accountEmail}</p>
                </div>
              </div>

              {/* 同步统计 */}
              <div className="grid grid-cols-2 gap-3">
                <div className="p-3 bg-gray-50 rounded-lg border">
                  <p className="text-xs text-gray-600">已同步事件</p>
                  <p className="mt-1 text-2xl font-semibold text-gray-900">
                    {status.syncedEventCount || 0}
                  </p>
                </div>
                <div className="p-3 bg-gray-50 rounded-lg border">
                  <p className="text-xs text-gray-600">上次同步</p>
                  <p className="mt-1 text-sm font-medium text-gray-900">
                    {status.lastSyncTime ? formatTime(status.lastSyncTime) : '从未同步'}
                  </p>
                </div>
              </div>

              {/* 同步按钮 */}
              <Button
                onClick={handleSync}
                disabled={status.syncing}
                className="w-full bg-blue-600 hover:bg-blue-700"
              >
                {status.syncing ? (
                  <>
                    <Loader2 className="size-4 mr-2 animate-spin" />
                    同步中...
                  </>
                ) : (
                  <>
                    <Sync className="size-4 mr-2" />
                    立即同步
                  </>
                )}
              </Button>

              {/* 同步结果 */}
              {syncResult && (
                <div className={`p-4 rounded-lg border ${
                  syncResult.failed === 0
                    ? 'bg-green-50 border-green-200'
                    : 'bg-yellow-50 border-yellow-200'
                }`}>
                  <div className="flex items-start gap-2">
                    {syncResult.failed === 0 ? (
                      <CheckCircle className="size-5 text-green-600 mt-0.5" />
                    ) : (
                      <AlertCircle className="size-5 text-yellow-600 mt-0.5" />
                    )}
                    <div className="flex-1">
                      <p className={`font-medium ${
                        syncResult.failed === 0 ? 'text-green-900' : 'text-yellow-900'
                      }`}>
                        {syncResult.message}
                      </p>
                      <div className="mt-2 text-sm">
                        <p className={syncResult.failed === 0 ? 'text-green-700' : 'text-yellow-700'}>
                          成功: {syncResult.synced} 个事件
                        </p>
                        {syncResult.failed > 0 && (
                          <p className="text-yellow-700">
                            失败: {syncResult.failed} 个事件
                          </p>
                        )}
                      </div>
                      {syncResult.errors && syncResult.errors.length > 0 && (
                        <details className="mt-3">
                          <summary className="text-sm font-medium cursor-pointer text-yellow-800">
                            查看错误详情
                          </summary>
                          <ul className="mt-2 space-y-1 text-sm text-yellow-700">
                            {syncResult.errors.map((error, index) => (
                              <li key={index}>
                                事件 {error.eventId}: {error.error}
                              </li>
                            ))}
                          </ul>
                        </details>
                      )}
                    </div>
                  </div>
                </div>
              )}

              {/* 断开连接按钮 */}
              <Button
                onClick={handleDisconnect}
                variant="outline"
                className="w-full text-gray-600 hover:text-gray-900"
              >
                断开连接
              </Button>
            </div>
          )}
        </div>
      </DialogContent>
    </Dialog>
  )
}
```

---

## 🔄 集成到 Sidebar

### 修改 Sidebar.tsx

```typescript
// components/Sidebar.tsx

// 1. 添加动态导入
const CalendarSyncDialog = dynamic(
  () => import("@/components/CalendarSyncDialog").then(m => m.CalendarSyncDialog),
  { ssr: false }
)

// 2. 修改 handleCalendarSyncClick 函数
const handleCalendarSyncClick = () => {
  if (isLoading) return

  // Paywall: Non-Pro users see upgrade modal
  if (!isPro) {
    setUpgradeFeature("Calendar Sync")
    setShowUpgradeModal(true)
    return
  }

  // Pro users: Open Calendar Sync dialog
  setShowCalendarSyncDialog(true)
}

// 3. 添加状态
const [showCalendarSyncDialog, setShowCalendarSyncDialog] = useState(false)

// 4. 在组件中添加 Dialog（在返回的 JSX 中）
<CalendarSyncDialog
  open={showCalendarSyncDialog}
  onOpenChange={setShowCalendarSyncDialog}
/>
```

---

## 🎯 用户体验优化

### 1. 进度指示器

对于大量事件的同步，显示实时进度：

```typescript
// 使用 Server-Sent Events (SSE) 实现实时进度

// 前端
const handleSyncWithProgress = async () => {
  const eventSource = new EventSource('/api/calendar/sync/stream')

  eventSource.onmessage = (event) => {
    const data = JSON.parse(event.data)

    switch (data.type) {
      case 'progress':
        setCurrentProgress(data.progress)
        setSyncedCount(data.synced)
        break
      case 'complete':
        eventSource.close()
        setSyncResult(data.result)
        break
      case 'error':
        eventSource.close()
        toast.error(data.error)
        break
    }
  }
}

// 显示进度条
<div className="w-full bg-gray-200 rounded-full h-2">
  <div
    className="bg-blue-600 h-2 rounded-full transition-all"
    style={{ width: `${currentProgress}%` }}
  />
</div>
<p className="text-sm text-gray-600 mt-2">
  已同步 {syncedCount} / {totalEvents} 个事件
</p>
```

### 2. 同步历史记录

```typescript
// 显示最近的同步记录
const [syncHistory, setSyncHistory] = useState<SyncHistoryItem[]>([])

useEffect(() => {
  const fetchHistory = async () => {
    const response = await fetch('/api/calendar/history')
    const data = await response.json()
    setSyncHistory(data.history)
  }

  if (status.connected) {
    fetchHistory()
  }
}, [status.connected])

// 显示
{syncHistory.length > 0 && (
  <div className="mt-4">
    <h4 className="text-sm font-medium text-gray-900 mb-2">同步历史</h4>
    <div className="space-y-2">
      {syncHistory.slice(0, 5).map((item, index) => (
        <div key={index} className="flex items-center justify-between text-sm">
          <span className="text-gray-600">
            {new Date(item.timestamp).toLocaleString()}
          </span>
          <span className={item.success ? 'text-green-600' : 'text-red-600'}>
            {item.success ? `+${item.synced} 个事件` : '失败'}
          </span>
        </div>
      ))}
    </div>
  </div>
)}
```

### 3. 自动检测 URL 参数

处理 OAuth 回调：

```typescript
// components/CalendarSyncDialog.tsx
useEffect(() => {
  // 检查 URL 参数
  const params = new URLSearchParams(window.location.search)
  const calendarSync = params.get('calendar_sync')

  if (calendarSync === 'success') {
    toast.success('Google Calendar 连接成功！')
    // 清除 URL 参数
    window.history.replaceState({}, '', window.location.pathname)
    // 重新检查状态
    checkStatus()
  } else if (calendarSync === 'error') {
    toast.error('连接 Google Calendar 失败，请重试')
    window.history.replaceState({}, '', window.location.pathname)
  }
}, [])
```

---

## 🎨 样式定制

### 使用 shadcn/ui 组件

```typescript
// 确保安装了需要的组件
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"
import { Progress } from "@/components/ui/progress"  // 进度条
import { Badge } from "@/components/ui/badge"        // 状态标签
```

### 自定义颜色主题

```css
/* globals.css */
@layer components {
  .sync-success {
    @apply bg-green-50 border-green-200 text-green-900;
  }

  .sync-error {
    @apply bg-red-50 border-red-200 text-red-900;
  }

  .sync-warning {
    @apply bg-yellow-50 border-yellow-200 text-yellow-900;
  }
}
```

---

## 📱 响应式设计

### 移动端适配

```typescript
// 检测设备
const isMobile = useMediaQuery('(max-width: 768px)')

// 调整对话框宽度
<DialogContent className={isMobile ? "w-full max-w-full" : "sm:max-w-[500px]"}>
```

### 触摸友好的按钮

```typescript
<Button
  onClick={handleSync}
  className="w-full min-h-[44px] bg-blue-600 hover:bg-blue-700"  // 最小高度 44px
>
  <Sync className="size-4 mr-2" />
  立即同步
</Button>
```

---

## 🧪 测试清单

### UI 测试
- [ ] 对话框打开/关闭正常
- [ ] 连接按钮显示正确
- [ ] 同步按钮显示正确
- [ ] 状态信息显示正确
- [ ] 错误消息显示正确
- [ ] 加载状态显示正确

### 功能测试
- [ ] 未连接时显示连接引导
- [ ] 已连接时显示同步界面
- [ ] 点击"连接"跳转到 Google 授权页面
- [ ] 点击"同步"执行同步操作
- [ ] 点击"断开"删除连接
- [ ] Pro 限制正常工作
- [ ] OAuth 回调处理正确

### 响应式测试
- [ ] 桌面端显示正常
- [ ] 移动端显示正常
- [ ] 对话框自适应屏幕大小
- [ ] 按钮大小适合触摸

---

## 🎯 最佳实践

### 1. 加载状态
```typescript
// ❌ 不好的做法
{loading && <div>加载中...</div>}

// ✅ 好的做法
{loading && (
  <div className="flex flex-col items-center justify-center py-8">
    <Loader2 className="size-8 animate-spin text-blue-600" />
    <p className="mt-4 text-sm text-gray-600">加载中...</p>
  </div>
)}
```

### 2. 错误处理
```typescript
// ❌ 不好的做法
catch (error) {
  console.error(error)
}

// ✅ 好的做法
catch (error) {
  console.error('Failed to sync:', error)
  toast.error('同步失败，请稍后重试')
  setStatus(prev => ({ ...prev, syncing: false }))
}
```

### 3. 状态更新
```typescript
// ❌ 不好的做法（直接修改状态）
status.syncing = true

// ✅ 好的做法（创建新对象）
setStatus(prev => ({ ...prev, syncing: true }))
```

---

## 📚 相关组件

### UpgradeModal
```typescript
// 当非 Pro 用户点击时显示
<UpgradeModal
  open={showUpgradeModal}
  onOpenChange={setShowUpgradeModal}
  feature="Calendar Sync"
/>
```

### Toast 通知
```typescript
import { toast } from "sonner"

// 成功
toast.success('同步成功！')

// 错误
toast.error('同步失败，请稍后重试')

// 加载
toast.loading('正在同步...', { duration: 1000 })
```

---

## 🎯 总结

### 前端 UI 实现的关键点

1. **状态管理**:
   - 连接状态（已连接/未连接）
   - 同步状态（同步中/空闲）
   - 同步结果（成功/失败）

2. **用户体验**:
   - 清晰的状态反馈
   - 友好的错误提示
   - 实时的进度显示

3. **Pro 限制**:
   - 非用户显示升级提示
   - Pro 用户显示完整功能

4. **响应式设计**:
   - 桌面端和移动端适配
   - 触摸友好的交互

---

**完成！现在你已经了解了 Calendar Sync 功能的完整实现细节。**

下一步：开始实现代码，从数据库设置开始。
